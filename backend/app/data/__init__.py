"""Layer 1 — data acquisition & preparation (ARCHITECTURE.md §2).

May import from ``app.core`` only; the import-linter contract enforces the
direction. Knows nothing about models, residuals, or interpretation.
"""
